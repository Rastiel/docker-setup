# Ubuntu için Docker Otomatik Kurulum Scripti

Bu repo, Ubuntu sunucularda Docker'ı tek komutla kurmak için hazırlanmıştır.

## Kullanım

```bash
git clone https://github.com/kullaniciadi/docker-setup.git
cd docker-setup
chmod +x install.sh
./install.sh
